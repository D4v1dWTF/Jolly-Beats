<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Upload Song - Jolly Beats</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="container">
    <header>
      <button class="font-size-toggle" onclick="toggleFontSize()">
        <span class="size-a" id="sizeSmall">A</span>
        <span class="size-a" id="sizeNormal">A</span>
        <span class="size-a" id="sizeLarge">A</span>
      </button>
      <h1><a href="/">Jolly Beats</a></h1>
      <nav>
        <a href="/songs">My Songs</a>
        <a href="/songs/browse">World</a>
        <a href="/playlists">Playlists</a>
        <a href="/forum">Forum</a>
        <a href="/profile">Profile</a>
        <a href="/logout" class="btn-logout">Logout (<%= user.username %>)</a>
      </nav>
    </header>

    <main class="content">
      <div class="form-container">
        <h2>Upload New Song</h2>
        
        <% if (error) { %>
          <div class="error-message"><%= error %></div>
        <% } %>

        <form action="/songs/upload" method="POST" enctype="multipart/form-data" class="form">
          <div class="form-group">
            <label for="title">Title: *</label>
            <input type="text" id="title" name="title" required>
          </div>

          <div class="form-group">
            <label for="artist">Artist: *</label>
            <input type="text" id="artist" name="artist" required>
          </div>

          <div class="form-group">
            <label for="songFile">Music File: * (mp3, wav, ogg, m4a)</label>
            <input type="file" id="songFile" name="songFile" accept=".mp3,.wav,.ogg,.m4a" required>
            <small>Maximum file size: 10MB</small>
          </div>

          <div class="form-buttons">
            <button type="submit" class="btn btn-primary">Upload Song</button>
            <a href="/songs" class="btn btn-secondary">Cancel</a>
          </div>
        </form>
      </div>
    </main>

    <footer>
      <p>&copy; 2025 Jolly Beats</p>
    </footer>
  </div>
  <script src="/js/fontsize.js"></script>
</body>
</html>

